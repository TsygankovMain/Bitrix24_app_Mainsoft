/**
 * Признаки готовности функций, которые уже выкачены в прод, но временно
 * скрыты от сотрудников.
 *
 * Скрывать точку входа, а не откатывать выкат: откат унёс бы вместе с формой
 * и не связанные с ней исправления того же выката (выбор стадии проекта,
 * принудительное обновление доски). Флаг держится в одном месте, чтобы
 * возврат функции был правкой одной строки, а не поиском всех кнопок.
 */

/**
 * Форма «Создать проект» (docs/superpowers/specs/2026-07-28-create-project-button-design.md).
 *
 * Выключена 29.07.2026 по итогам первого дня в проде. Что сломано:
 *  1. Введённое название компании не видно на экране после закрытия списка —
 *     поле показывает "Компания не выбрана" всегда, когда компания не выбрана
 *     ИЗ СПИСКА, а у новой компании выбирать нечего. Текст при этом жив в
 *     состоянии формы, но человек считает ввод потерянным.
 *  2. Нет действия «создать компанию с этим названием»: при пустом результате
 *     поиска показывается неактивная надпись «Ничего не найдено», хотя
 *     подсказка под полем обещает, что введённое название станет именем новой
 *     компании. Обещание есть, механизма нет.
 *  3. Escape в строке поиска закрывает не список, а всё модальное окно —
 *     обработчик гасит список, но не останавливает событие, и его следом
 *     ловит сам диалог.
 *
 * Включать обратно только после того, как сценарий «компании нет в базе —
 * завожу новую» пройден руками от начала до конца: ревью проверяло связку
 * идентификатора с именем и этот путь пропустило.
 */
export const CREATE_PROJECT_BUTTON_ENABLED = false
