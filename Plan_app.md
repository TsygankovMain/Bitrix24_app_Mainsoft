# Планирование разработки приложения (Plan_app)

## 1. Главная страница (Dashboard)
**Цель:** Создать точку входа в приложение с навигацией в виде плиток, соответствующую дизайн-коду "Зефир" (Bitrix24 UI).

### 1.1 UI Структура
*   **Макет:** Сетка (Grid) из 6 плиток.
*   **Элементы:**
    1.  **Отчет 1** (Тестовый отчет для отладки)
    2.  **Отчет 2** (Заглушка)
    3.  **Отчет 3** (Заглушка)
    4.  **Отчет 4** (Заглушка)
    5.  **Настройки** (Переход на страницу настроек)
    6.  **Обратная связь** (Кнопка вызова попапа)

### 1.2 Компоненты Плитки
*   Иконка (Использование стандартных иконок B24 или SVG).
*   Название отчета/раздела.
*   Интерактивность: Hover-эффекты, кликабельность.

## 2. Форма Обратной связи (Support/Feature Request)
**Цель:** Сбор заявок на ТП и улучшение функционала.

### 2.1 Реализация
*   **Механизм:** Popup окно (используя `B24Popup` или модальное окно UI Kit).
*   **Поля формы:**
    *   Тема обращения.
    *   Описание/Текст.
    *   Тип: "Ошибка" / "Улучшение".
    *   Кнопка "Отправить".
*   **Логика:** При отправке данные пока можно просто логировать или отправлять на заглушку API.

## 3. Отладка Единого Отчета
**Цель:** Реализовать один отчет для отладки полного цикла данных (Бэкенд -> Фронтенд).

### 3.1 Перенос функционала
*   Текущую логику из `index.client.vue` (отображение таблиц отчетов) перенести на отдельную страницу (например, `/reports/debug`).
*   Привязать первую плитку "Отчет 1" к этому маршруту.

### 3.2 Техническая реализация
*   Убедиться, что данные с бэкенда (`apiStore.getReport...`) загружаются корректно.
*   Отобразить данные в таблице (как сейчас реализовано `EmployeeProjectTable`).
*   Этот отчет станет шаблоном для остальных.

## 4. Этапы реализации
1.  **Рефакторинг `index.client.vue`**: Создание сетки меню.
2.  **Создание страницы отчета**: Перенос текущего кода отчетов на `pages/reports/debug.vue`.
3.  **Реализация Попапа**: Верстка формы обратной связи.
4.  **Стилизация**: Приведение к гайдлайнам "Зефир" (цвета, отступы, шрифты).
