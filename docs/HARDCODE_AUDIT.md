# Аудит хардкода: подготовка к маркетплейсу

> **Дата**: 19.02.2026
> **Статус**: ✅ Исправлено (Ready for Publish)

---

## Найденные проблемы

### 🟢 1. `embedded.vue` — полный набор захардкоженных кодов полей [ИСПРАВЛЕНО]

**Решение:** Внедрен `fieldConfigStore`, загружающий ID полей из `app.option`. Все поля теперь динамические.

### 🟢 2. `project-report.client.vue` — дублирование захардкоженных полей [ИСПРАВЛЕНО]

**Решение:** Внедрен `fieldConfigStore`. Компонент переписан на `b24jssdk` и использует динамическую конфигурацию.

### 🟡 3. `embedded.vue` и `task.vue` — ставка за час

**Проблема:** Фиксированная ставка 3000 руб/час.
**Статус:** Осталось как есть (не блокирует публикацию, можно вынести в настройки в будущем).

### 🟢 4. `embedded.vue` — коды полей задач (ИНН) [ИСПРАВЛЕНО]

**Решение:** Поля `our_inn` и `client_inn` добавлены в общий механизм маппинга (`settings/mapping.client.vue`) и `fieldConfigStore`. Их ID теперь динамические.

---

## Итог

Все критические (🔴) проблемы с хардкодом устранены. Приложение готово к установке на любой портал.
